<%- include('../layout', { title: 'Thêm bài viết', body: `
    <div class="content">

        <h1>➕ Thêm bài viết mới</h1>
        <p>Sử dụng biểu mẫu dưới đây để nhập thông tin và tạo bài viết mới.</p>

        <div class="card" style="border-left: 4px solid #764ba2; margin-bottom: 2rem;">
            <h3>ℹ️ Lưu ý trước khi đăng</h3>
            <ul>
                <li>Tiêu đề và nội dung bài viết là **bắt buộc**.</li>
                <li>Tác giả sẽ mặc định là "Anonymous" nếu bạn không nhập.</li>
                <li>Bài viết sẽ được lưu trữ vào PostgreSQL database thông qua API.</li>
            </ul>
        </div>
        
        <div id="statusMessage" class="status" style="display: none;"></div>


        <form id="addPostForm" action="/api/posts" method="POST">
            
            <div class="card" style="border-left: 4px solid #667eea; padding-bottom: 1rem;">
                <div class="form-group">
                    <label for="postTitle" class="form-label">Tiêu đề bài viết:</label>
                    <input 
                        type="text" 
                        id="postTitle" 
                        name="title" 
                        class="form-control" 
                        placeholder="Ví dụ: Giới thiệu về Node.js và Express.js" 
                        required
                    >
                </div>
                
                <div class="form-group">
                    <label for="postAuthor" class="form-label">Tác giả:</label>
                    <input 
                        type="text" 
                        id="postAuthor" 
                        name="author" 
                        class="form-control" 
                        placeholder="Nhập tên của bạn hoặc để trống cho 'Anonymous'"
                    >
                </div>
                
                <div class="form-group">
                    <label for="postContent" class="form-label">Nội dung bài viết:</label>
                    <textarea 
                        id="postContent" 
                        name="content" 
                        class="form-control" 
                        rows="15" 
                        placeholder="Bắt đầu viết nội dung bài viết của bạn tại đây..." 
                        required
                    ></textarea>
                </div>

                <div class="form-group" style="text-align: right; margin-top: 2rem; margin-bottom: 0;">
                    <button type="submit" class="btn">
                        Tạo Bài Viết
                    </button>
                    
                    <button type="button" class="btn btn-secondary" onclick="document.getElementById('addPostForm').reset()">
                        Xóa Form
                    </button>

                    <a href="/posts" class="btn btn-secondary" style="background: linear-gradient(45deg, #495057, #6c757d); margin-left: 10px;">
                        Quay lại Danh sách
                    </a>
                </div>
            </div>
        </form>
    </div>
` }) %>